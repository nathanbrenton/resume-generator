// Targeted application roles are loaded from one file per application preset.
// This keeps future job-specific patches isolated and avoids a growing monolithic catalog.
const targetedApplicationRoleDefinitions = [];
